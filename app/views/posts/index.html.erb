<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>instagram</title>
  </head>
  <body>
    <h1>hi there.</h1>
    <a><%= link_to "Post a pic", new_post_path, :class => 'btn btn-success' %></a>
    <% @posts.each do |post| %>
    <br>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.body %></td>
    </tr>
  <% end %>
  </body>
</html>
